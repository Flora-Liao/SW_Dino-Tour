const scheduleIntroInit = {
    scheduleIntro: '',
    tagText: []
}

export function scheduleIntro(state = scheduleIntroInit, action) {
    switch (action.type) {
        default:
            return state;
    }
}
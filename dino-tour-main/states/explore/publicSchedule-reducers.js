export function publicSchedule(state = '', action) {
    switch (action.type) {
        default:
            return state;
    }
}

export function publicScheduleInfo(state = '', action) {
    switch (action.type) {
        default:
            return state;
    }
}

export function spotInfo(state = '', action) {
    switch (action.type) {
        default:
            return state;
    }
}